(ns solita.etp.service.pdf-sign
  (:require [clojure.java.io :as io])
  (:import (org.apache.pdfbox.pdmodel PDDocument)
           (org.apache.xmpbox.xml DomXmpParser)))

#_(defn sign-pdf [^PDDocument pdf]
  (let [hmm .Verify
        ])




  )